# Catalogo "postgresql": permite consultar la base relacional original desde Trino.
# Muy util para CUADRATURA (comparar conteos/importes origen vs lakehouse).
connector.name=postgresql
connection-url=jdbc:postgresql://postgres-source:5432/olist
connection-user=olist
connection-password=olist
