# Orquestacion / extraccion
# Pin exacto de Airflow: sin esto, pip actualiza a Airflow 3.x (el provider
# Spark >=4.9 lo exige) y rompe el CLI y los imports del DAG (escritos para 2.x).
apache-airflow==2.9.3
apache-airflow-providers-apache-spark==4.1.2
# Airflow 2.9.3 exige sqlalchemy<2.0 (el pin 2.x provocaba el salto a Airflow 3).
# El extract solo usa create_engine/text()/chunksize, compatible con 1.4.
sqlalchemy>=1.4.36,<2.0
psycopg2-binary>=2.9.9
pandas>=2.1,<2.3
boto3>=1.34,<1.36
minio>=7.2,<7.3

# Procesamiento distribuido
pyspark==3.5.1

# Cliente Trino (sincronizacion de particiones y consultas ad-hoc)
trino>=0.328,<0.333

# Transformacion, tests y documentacion
dbt-core>=1.8,<1.9
dbt-trino>=1.8,<1.9

# Calidad / utilidades
python-dotenv>=1.0
pytest>=8.0
